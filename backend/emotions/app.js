var request = require("sync-request");

// TODO: params = {...?}
exports.main = function(params) {

    // something something call the relevant API

    var res = request(type, url, options);

    return {};
}

